{{#each usersPosts}}

  <div class="card my-1 mx-3">
    <h5 class="card-header d-flex justify-content-between">{{title}}
      <span class="fs-6 fw-lighter">Created by:
        {{this.user.username}}
        at

        {{format_time date}}</span></h5>

    <div class="card-body">

      <p class="card-text">
        {{content}}
      </p>
    </div>
  </div>

{{/each}}